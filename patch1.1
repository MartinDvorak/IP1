*** VTFParser.py	2017-10-04 13:24:59.962803028 +0200
--- VTFParser1.1.py	2017-10-08 14:37:02.198907435 +0200
***************
*** 152,157 ****
--- 152,168 ----
              continue
  
          return tokenize(line)
+ ###########################################
+ def check_required(automata_type, body_dict):
+     ''' Checking required values in input file. look README.md for required values.
+     '''
+     #required formate: (['type of atomata'] , ['requird values'])
+     required = [ (['NFA','DFA','SFA','NFT','SFT','DPA'],['Initial','Final']), (['NTA'],['Root']), (['NFA-BDD'],['Symbol-Vars']),(['NFA-BDD-FULL'],['Symbol-Vars','State-Vars']) ]
+     for tupl in required:
+         if automata_type in tupl[0]:
+             for i in tupl[1]:
+                 if not i in body_dict:
+                     raise Exception("Missing required value: " + str(i))         
  
  ###########################################
  def parsevtf(fd):
***************
*** 193,198 ****
--- 204,211 ----
      if not type_parsed:
          raise Exception("Could not find a @TYPE directive")
  
+     check_required(result.type, result.dict)
+ 
      return result
  
  ##########################
